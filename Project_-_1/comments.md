add subtle background to get rid of whitespace

dynamically change background images for cards based on type of venue

add a created by element at the beginning. Mouse over reveals names.

add something fun!